# Core runtime + everything the test suite needs.
fastapi>=0.115
httpx>=0.27
pydantic>=2.0
uvicorn>=0.30
pytest>=8.0

# --- Optional, NOT needed for tests ---
# Cross-encoder re-ranking and fine-tuning (heavy: pulls in torch).
#   pip install "sentence-transformers>=3.0"
# Synthetic training-data generation via Gemini.
#   pip install "google-genai>=0.3"
